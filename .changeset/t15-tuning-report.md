---
'@geoverse-sar/kernel': patch
---

L1 自调优执行器（RFC-0009 T15）：`createTuningReport({ audit, monitor, catalog })` + `formatTuningReport`——ErrorMonitor+audit 传感器数据 → schema（同路径反复校验失败）/description（capability_not_found 幻觉名单）/usage（失败率异常）三类修订建议（均带 evidence）+ audit 成功轨迹 few-shot 素材。确定性零 LLM，报告可复现可 diff；红线"进化数据不进化代码"——修订由人执行后过正常门禁。
