{
  "name": "@geoverse-sar/skill",
  "version": "0.1.0",
  "description": "AI entry for the SAR kernel: project CapabilityDescriptors into Claude tool specs (toToolSpecs) and route tool calls back through the single invoke funnel (handleToolCall).",
  "type": "module",
  "license": "MIT",
  "author": "DIY Liu",
  "keywords": [
    "ai",
    "tool-use",
    "claude",
    "runtime",
    "geoverse"
  ],
  "files": [
    "dist",
    "!dist/**/*.map"
  ],
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "module": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "sideEffects": false,
  "publishConfig": {
    "access": "public"
  },
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "vite build",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "@geoverse-sar/kernel": "workspace:*"
  },
  "devDependencies": {
    "@geoverse-sar/capabilities-records": "workspace:*",
    "@geoverse-sar/engine-memory": "workspace:*",
    "vite-plugin-dts": "^5.0.2"
  }
}
